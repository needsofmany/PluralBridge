13.1  Confirm clean Task 13 starting point
      Verify Task 12 is committed, branch is correct, and working tree is clean.
      Done when:
        branch = feature/pb-next-account-model-010
        status is clean
        HEAD shows Add membership resolver

13.2  Add CurrentSystem helper record
      Add a private controller-local record shape for the current system resolved through membership.
      Done when controller code can hold:
        SystemId
        SystemName
        SystemMembershipId

13.3  Add system-by-id SQL helper method
      Add a private async helper method that reads one pb_systems row by SystemId.
      Done when controller code can read system metadata using a supplied SystemId.

13.4  Add current-system resolver helper
      Add a private async helper method that chooses the current system from membershipAccess.
      Done when controller code resolves the current SystemId from membershipAccess instead of reading the first system row.

13.5  Wire current-system resolver into /api/me
      After membershipAccess resolves, call the current-system resolver.
      Done when /api/me has currentSystem available before returning JSON.

13.6  Return current system in /api/me JSON
      Add currentSystem to the existing /api/me response.
      Done when /api/me includes:
        currentAccount
        membershipAccess
        currentSystem
        counts

13.7  Build the REST API
      Build the solution in VS2022.
      Done when:
        Build succeeded

13.8  Verify /api/me current-system output
      Run the API, log in, and open /api/me.
      Done when /api/me shows:
        currentSystem.SystemId matches membershipAccess[0].SystemId
        existing counts are still present

13.9  Review the Task 13 diff
      Confirm the change is limited to MeController current-system resolver wiring.
      Done when the diff shows:
        no new endpoint
        no new controller
        no Models folder
        no DbContext
        existing /api/me counts preserved

13.10 Commit Task 13
      Commit the bounded current-system resolver change.
      Done when the branch contains a commit named:
        Add current system resolver