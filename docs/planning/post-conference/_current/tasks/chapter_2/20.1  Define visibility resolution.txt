20.1  Define visibility resolution rule intent
      Add the Task 20 note that defines the rule:
        PB visibility scope first
        SP privacy bucket fallback only when PB visibility is absent
        no runtime enforcement change in Task 20
      Done when:
        PB-first / SP-fallback is written down
        SP buckets remain source fidelity data
        PB visibility scopes remain PB runtime authority
        Task 20 does not activate enforcement

20.2  Add visibility resolution rule design record
      Add a small design file under the Chapter 2 current planning folder.
      Done when it states:
        resource visibility resolution checks PB visibility scope first
        absent PB visibility falls back to SP privacy bucket behavior
        conflicting PB and SP values prefer PB
        absence of both means existing behavior remains unchanged for now

20.3  Add visibility resolution verification script
      Add a SQL script under:
        scripts/sqlserver/chap2_safe_spine/scripts
      Done when the script verifies:
        pb_visibility_scopes exists
        pb_visibility_scope_members exists
        pb_privacy_buckets exists
        no FK joins PB visibility scopes to SP privacy buckets
        fallback rule is documented, not enforced by schema

20.4  Run visibility resolution verification script
      Run the saved script in SSMS against PluralBridgeChap2SafeSpine.
      Done when:
        TASK20_PB_VISIBILITY_TABLES_EXIST=1
        TASK20_SP_PRIVACY_BUCKETS_EXIST=1
        TASK20_NO_DIRECT_PB_SP_COUPLING=1
        TASK20_RULE_IS_DESIGN_ONLY=1
        TASK20_OK

20.5  Commit Task 20
      Commit the bounded design/verification files.
      Done when the branch contains a commit named:
        Add visibility resolution rule
        no remaining Task 20 files in git status

20.6  Push Task 20
      Push feature/pb-next-account-model-010 to origin.
      Done when:
        remote branch has Add visibility resolution rule
        no remaining Task 20 files in git status