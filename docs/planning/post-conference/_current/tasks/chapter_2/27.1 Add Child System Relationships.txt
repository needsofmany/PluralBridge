Task 27 is **Add child system relationship schema**. This task adds database support for root/child system hierarchy using pb_system_relationships. The existing pb_systems table remains the system entity table. Membership remains attached to the system boundary, and child system access remains a future inherited-access rule.

27.1  Define child system relationship intent
      Add the Task 27 note that defines child systems as a hierarchy under pb_systems.
      Done when:
        child systems are named
        pb_system_relationships is named
        pb_systems remains the system entity table
        membership behavior is unchanged
        /api/me behavior is unchanged

27.2  Add child system relationship design record
      Add a design file that defines the system hierarchy shape.
      Done when it states:
        every system row has one hierarchy row
        root system rows have ParentSystemId = NULL
        child system rows point to a parent SystemId
        SystemLevel stores tree depth
        SystemLevel = 1 means root
        SystemLevel = 2 means child
        SystemLevel = 3 means grandchild
        no current runtime behavior is activated

27.3  Add pb_system_relationships table
      Run the pb_system_relationships CREATE TABLE SQL in SSMS against PluralBridgeChap2SafeSpine.
      Done when:
        dbo.pb_system_relationships exists
        SystemRelationshipId exists
        SystemId exists
        ParentSystemId exists
        SystemLevel exists
        CreatedAtUtc exists
        UpdatedAtUtc exists

27.4  Add system relationship constraints
      Run the system relationship constraint SQL in SSMS against PluralBridgeChap2SafeSpine.
      Done when:
        pb_system_relationships has a primary key on SystemRelationshipId
        pb_system_relationships has a unique constraint on SystemId
        SystemId has a foreign key to pb_systems
        ParentSystemId has a foreign key to pb_systems
        ParentSystemId cannot equal SystemId
        SystemLevel must be greater than or equal to 1

27.5  Seed current root system relationship
      Run SQL in SSMS to add the current root system relationship row.
      Done when:
        current root SystemId has one pb_system_relationships row
        ParentSystemId is NULL
        SystemLevel = 1
        no duplicate root relationship row is created

27.6  Add system relationship verification SQL
      Add a saved SQL verification script under:
        scripts/sqlserver/chap2_safe_spine/scripts
      Done when the script verifies:
        pb_system_relationships exists
        expected columns exist
        expected primary key exists
        expected unique constraint exists
        expected foreign keys exist
        expected check constraints exist
        current root relationship row exists
        system relationship support is schema-only
        no runtime behavior is activated

27.7  Run system relationship verification SQL
      Run the saved verification script in SSMS against PluralBridgeChap2SafeSpine.
      Done when:
        TASK27_SYSTEM_RELATIONSHIPS_TABLE_EXISTS=1
        TASK27_EXPECTED_COLUMNS=1
        TASK27_PK_EXISTS=1
        TASK27_UNIQUE_SYSTEM_EXISTS=1
        TASK27_FK_COUNT is correct
        TASK27_CHECK_COUNT is correct
        TASK27_ROOT_ROW_EXISTS=1
        TASK27_SCHEMA_ONLY=1
        TASK27_OK

27.8  Verify /api/me owner path still works
      Run /api/me and confirm system relationship schema did not change the current owner path.
      Done when:
        currentAccount.email = demo@thepluralbridge.local
        membershipAccess[0].membershipStatus.statusName = Active
        membershipAccess[0].roles[0].roleName = Owner
        currentSystem.systemId = 826d77cf-8b1a-a301-4efe-1113e5a17e88
        members = 49
        privacyBuckets = 2
        customFields = 7
        frontHistory = 886

27.9  Add child system relationship verification record
      Add a Task 27 verification file recording the system relationship schema result.
      Done when it states:
        pb_system_relationships exists
        current root system relationship row exists
        constraints exist
        pb_systems remains unchanged
        membership behavior remains unchanged
        child system access remains future work
        /api/me owner path remains unchanged

27.10  Commit Task 27
      Commit the bounded child system relationship schema change.
      Done when the branch contains a commit named:
        Add child system relationship schema
        no remaining Task 27 files in git status

27.11  Push Task 27
      Push feature/pb-next-account-model-010 to origin.
      Done when:
        remote branch has Add child system relationship schema
        no remaining Task 27 files in git status