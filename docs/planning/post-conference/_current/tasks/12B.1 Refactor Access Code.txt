Step 12B should be split like this:

12B.1  Confirm AccessContextHelper exists
       Confirm the new helper class exists at:
         api/PluralBridge.Api/PluralBridge.Api/Controllers/AccessContextHelper.cs
       Done when the file exists in the Controllers folder and uses:
         namespace PluralBridge.Api.Controllers;

12B.2  Add helper class imports
       Add only the imports needed by the helper class.
       Done when AccessContextHelper.cs can use:
         SqlConnection
         SqlCommand
         SqlDataReader

12B.3  Move AccountStatus record
       Move AccountStatus from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can represent:
         AccountStatusId
         StatusName
         StatusDesc
         DisplayOrder
         IsActive

12B.4  Move Account record
       Move Account from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can represent:
         AccountId
         Email
         DisplayName
         AccountStatusId
         AccountStatus
         CreatedAtUtc
         UpdatedAtUtc

12B.5  Move Role record
       Move Role from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can represent:
         RoleId
         RoleName
         RoleDesc
         DisplayOrder
         IsActive

12B.6  Move MembershipStatus record
       Move MembershipStatus from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can represent:
         MembershipStatusId
         StatusName
         StatusDesc
         DisplayOrder
         IsActive

12B.7  Move SystemMembership record
       Move SystemMembership from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can represent:
         SystemMembershipId
         AccountId
         SystemId
         MembershipStatusId
         MembershipStatus
         Roles
         CreatedAtUtc
         UpdatedAtUtc

12B.8  Add AccessContext record
       Add one combined return shape for the reusable resolver.
       Done when AccessContextHelper can return:
         CurrentAccount
         MembershipAccess

12B.9  Move ReadAccountByEmailAsync
       Move the account SQL reader from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can read the current account and account status.

12B.10 Move ReadActiveMembershipAsync
       Move the active membership SQL reader from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can read the active system membership for one AccountId.

12B.11 Move ReadRolesForMembershipAsync
       Move the role SQL reader from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can attach roles to a system membership.

12B.12 Move ResolveCurrentAccountAsync
       Move the current-account resolver from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can resolve the configured current account.

12B.13 Move ResolveMembershipAccessAsync
       Move the membership-access resolver from MeController.cs into AccessContextHelper.cs.
       Done when AccessContextHelper can resolve membership access for the current account.

12B.14 Add ResolveCurrentAccessAsync
       Add one shared helper entry point that calls:
         ResolveCurrentAccountAsync
         ResolveMembershipAccessAsync
       Done when controllers can call one method to get:
         current account
         membership access

12B.15 Update MeController to call AccessContextHelper
       Replace direct calls to local resolver methods with:
         AccessContextHelper.ResolveCurrentAccessAsync(connection)
       Done when MeController gets currentAccount and membershipAccess from AccessContextHelper.

12B.16 Remove moved methods from MeController
       Remove the account and membership helper methods from MeController.cs.
       Keep:
         Get
         ReadCountsAsync
         ReadProofSystemAsync
         ProofSystem
       Done when MeController no longer owns shared access plumbing.

12B.17 Remove moved records from MeController
       Remove moved helper records from MeController.cs.
       Keep:
         ProofSystem
       Done when shared access records live only in AccessContextHelper.cs.

12B.18 Build the REST API
       Build the solution in VS2022.
       Done when:
         Build succeeded

12B.19 Verify /api/me output is preserved
       Run the API and open /api/me.
       Done when /api/me still shows:
         currentAccount
         membershipAccess
         Active
         Owner
         PluralBridgeChap2SafeSpine
         existing counts

12B.20 Review the Step 12B diff
       Confirm the refactor only moved shared access plumbing.
       Done when the diff shows:
         AccessContextHelper.cs added
         MeController.cs simplified
         no new endpoint
         no behavior change
         no Models folder
         no DbContext

12B.21 Commit Step 12B
       Commit the bounded helper extraction.
       Done when the branch contains a commit named:
         Extract access context helper
