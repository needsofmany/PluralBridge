Task 25 is **Final Chapter 2 verification**. The Chapter 2 build list says this task verifies the whole spine end-to-end. Demo account signs in, resolves membership, resolves system, passes authorization, and reads existing system data unchanged.

25.1  Confirm final verification starting point
      Confirm the branch is clean and positioned after Task 24.
      Done when:
        git status is clean
        current branch is feature/pb-next-account-model-010
        HEAD is Define expanded diagnostic trace mode

25.2  Add final verification intent note
      Add the Task 25 note that defines final Chapter 2 verification as an end-to-end spine check.
      Done when:
        final verification is named
        sign-in is included
        account resolution is included
        membership resolution is included
        current system resolution is included
        authorization is included
        unchanged data reads are included

25.3  Build the API solution
      Build api/PluralBridge.Api/PluralBridge.Api.sln in Visual Studio.
      Done when:
        Build succeeded
        0 errors

25.4  Run baseline trace tests
      Run RequestTraceContextTests in Visual Studio Test Explorer.
      Done when:
        RequestTraceContextTests pass
        0 failed

25.5  Run authorization test class
      Run AccessContextAuthorizationTests in Visual Studio Test Explorer.
      Done when:
        AccessContextAuthorizationTests pass
        0 failed

25.6  Verify protected browser sign-in
      Run the API and sign in through the protected browser app path.
      Done when:
        /login accepts the configured demo credentials
        /app/ loads after sign-in
        protected app files remain behind authorization
        logout remains available

25.7  Verify /api/me owner path still works
      Open /api/me and confirm the final owner-path values.
      Done when:
        currentAccount.email = demo@thepluralbridge.local
        membershipAccess[0].membershipStatus.statusName = Active
        membershipAccess[0].roles[0].roleName = Owner
        currentSystem.systemId = 826d77cf-8b1a-a301-4efe-1113e5a17e88
        members = 49
        privacyBuckets = 2
        customFields = 7
        frontHistory = 886

25.8  Verify local trace file still receives Level 1 trace
      Open the local rolling trace file after /api/me runs.
      Done when the local log file contains:
        PB_LEVEL1_TRACE
        account_resolution
        membership_resolution
        current_system_resolution
        authorization_check
        data_access
        TraceId
        CorrelationId

25.9  Add final verification record
      Add a Task 25 verification file recording the final Chapter 2 result.
      Done when it states:
        build passed
        trace tests passed
        authorization tests passed
        protected sign-in worked
        account resolved
        membership resolved
        current system resolved
        authorization allowed
        existing data counts remained unchanged
        Level 1 trace still persisted locally

25.10  Commit Task 25
      Commit the bounded final Chapter 2 verification record.
      Done when the branch contains a commit named:
        Verify Chapter 2 spine
        no remaining Task 25 files in git status

25.11  Push Task 25
      Push feature/pb-next-account-model-010 to origin.
      Done when:
        remote branch has Verify Chapter 2 spine
        no remaining Task 25 files in git status