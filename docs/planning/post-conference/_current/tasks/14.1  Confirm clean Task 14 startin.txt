14.1  Confirm clean Task 14 starting point
      Verify Task 13 is committed and pushed, branch is correct, and working tree is clean.
      Done when:
        branch = feature/pb-next-account-model-010
        status is clean
        HEAD shows Add current system resolver

14.2  Add current-system authorization helper
      Add one helper in AccessContextHelper that checks whether AccessContext has an active membership matching CurrentSystem.
      Done when helper checks:
        MembershipAccess contains matching SystemId
        MembershipAccess contains matching SystemMembershipId
        MembershipStatus.IsActive is true
        MembershipStatus.StatusName is Active

14.3  Wire authorization check into /api/me before data reads
      In MeController.Get(), call the authorization helper after accessContext resolves and before ReadCountsAsync / ReadProofSystemAsync.
      Done when /api/me does not read or return system data unless authorization passes.

14.4  Add unauthorized response path
      If authorization fails, return a clean 403 response from /api/me.
      Done when failed authorization exits before counts or proofSystem are read.

14.5  Preserve authorized /api/me response shape
      Keep the successful /api/me JSON shape from Task 13.
      Done when authorized /api/me still returns:
        currentAccount
        membershipAccess
        currentSystem
        proofSystem
        counts

14.6  Build the REST API
      Build the solution in VS2022.
      Done when:
        Build succeeded

14.7  Verify authorized /api/me still works
      Run the API and open /api/me with the seeded demo account.
      Done when:
        authorization passes
        currentSystem.systemId matches membershipAccess[0].systemId
        currentSystem.systemMembershipId matches membershipAccess[0].systemMembershipId
        existing counts are still present

14.8  Review the Task 14 diff
      Confirm the change is limited to the authorization boundary.
      Done when the diff shows:
        no new endpoint
        no new controller
        no Models folder
        no DbContext
        /api/me is gated by authorization before system data reads
        successful /api/me output remains intact

14.9  Commit Task 14
      Commit the bounded authorization boundary change.
      Done when the branch contains a commit named:
        Add authorization boundary

14.10  Push Task 14 commit to remote
       Push feature/pb-next-account-model-010 to origin.
       Done when:
         remote branch has Add authorization boundary
         local status is clean