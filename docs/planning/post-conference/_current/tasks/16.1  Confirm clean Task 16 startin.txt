16.1  Confirm clean Task 16 starting point
      Verify Task 14/comment cleanup is committed and pushed, branch is correct, and working tree is clean.
      Done when:
        branch = feature/pb-next-account-model-010
        status is clean
        HEAD shows Clean up access context comments

16.2  Locate existing test surface
      Identify whether the repo already has a test project or existing API/controller tests.
      Done when the test location is known, or we confirm there is no existing test project.

16.3  Choose the unauthorized-test seam
      Use the smallest available seam for the test:
        AccessContextHelper authorization helper if unit tests exist
        MeController /api/me behavior if API/controller tests exist
      Done when the test target is selected without adding new architecture.

16.4  Add missing-membership test case
      Add a test for an access context with no membership access.
      Done when the test proves authorization fails when MembershipAccess is empty.

16.5  Add wrong-system membership test case
      Add a test where membership exists but does not match currentSystem.SystemId.
      Done when the test proves authorization fails for the wrong system.

16.6  Add wrong-membership-id test case
      Add a test where SystemId matches but SystemMembershipId does not match currentSystem.SystemMembershipId.
      Done when the test proves authorization fails for mismatched membership identity.

16.7  Add inactive-membership test case
      Add a test where membership status is inactive or not Active.
      Done when the test proves authorization fails unless MembershipStatus.IsActive is true and StatusName is Active.

16.8  Add authorized-control test case
      Add the positive control test for the seeded Owner-style membership shape.
      Done when the test proves authorization succeeds for matching active membership.

16.9  Run tests
      Run the test project from Visual Studio/Test Explorer, or the existing repo test command if that is already the project convention.
      Done when:
        unauthorized tests pass
        authorized-control test passes
        existing tests still pass

16.10 Build the REST API
      Build the solution in VS2022.
      Done when:
        Build succeeded

16.11 Verify /api/me still works
      Run the API and open /api/me.
      Done when:
        authorized seeded demo path still returns 200
        currentSystem still matches membershipAccess
        counts are still present as a smoke check

16.12 Review the Task 16 diff
      Confirm the change is limited to unauthorized-test coverage.
      Done when the diff shows:
        no new endpoint
        no new controller
        no Models folder
        no DbContext
        no production authorization logic rewrite unless required by testability
        tests cover missing/wrong membership failure

16.13 Commit Task 16
      Commit the bounded unauthorized-test change.
      Done when the branch contains a commit named:
        Add unauthorized access tests

16.14 Push Task 16 commit to remote
      Push feature/pb-next-account-model-010 to origin.
      Done when:
        remote branch has Add unauthorized access tests
        local status is clean