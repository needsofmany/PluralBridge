Task 25 is Final Chapter 2 verification.

Purpose:
  Verify the Chapter 2 request spine end to end before closing the feature branch.

Verification scope:
  Demo sign-in is verified.
  Current account resolution is verified.
  Membership resolution is verified.
  Current system resolution is verified.
  Authorization is verified.
  Existing system data reads are verified unchanged.
  Level 1 diagnostic trace persistence is verified.

Done when:
  final verification is named
  sign-in is included
  account resolution is included
  membership resolution is included
  current system resolution is included
  authorization is included
  unchanged data reads are included
